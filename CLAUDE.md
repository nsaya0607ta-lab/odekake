# CLAUDE.md

このリポジトリで作業する際に必ず参照すること。

## アイテムキャッチ（`frenchie-catch-game.tsx`）に新しいアイテムを追加・調整するとき

**必ず `docs/minigame-time-balance.md` の「【2026-09-02 再確定】現実的キャッチ率モデルへの切り替え・
新アイテム追加時はまずここを見る」節を読んでから作業すること。** それより下にある同日の旧節・
2026-08-30以前の節は更新前の内容なので参照しない。

要点（詳細・計算式・検証手順は上記docsを参照）:

- プレイ時間・スコアのバランス検算は `node scripts/simulate-item-catch.mjs [試行回数] avoid` で行う。
  第4〜6引数（デフォルト: 時間増加系7種+おかえり80% / 通常時85% / 密集時50%）が現在の公式キャッチ率
  モデル。「画面に出た物は100%取れる」という前提には戻さないこと。
- 目標値は**Lv5で500秒超え10%・700秒超え3%・1000秒超え1%**（秒数平均ではなく裾の確率で管理する）。
- 新アイテムを追加すると図鑑プールが増えて既存アイテムの取得確率が薄まるため、時間増加系7種の
  出現重みで相殺する必要がある。手順は docs 側の「新しいキャラ・アイテムを追加するときの手順」節の通り。
- 裾の確率を主に動かすレバーは `other_okaeri`（おかえり）の出現重み。新アイテム追加で裾の確率が
  目標からズレたら、まずこの重みで調整する。
- 数値を変えたら `frenchie-catch-game.tsx` の `LV` / `ITEM_SPAWN_WEIGHTS` と、
  `src/lib/games/item-catch-skills.ts` のルールブック説明文の**両方**を直す。
- 新アイテムは `src/lib/collection/items.ts`（図鑑本体）への追加を忘れないこと（ここに無いとミニゲームの
  `itemPool` に一切登場しない静かなバグになる）。
- 数値を確定させたら、`docs/minigame-time-balance.md` の基準値・検証結果も一緒に更新すること。
