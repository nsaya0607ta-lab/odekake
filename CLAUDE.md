# CLAUDE.md

このリポジトリで作業する際に必ず参照すること。

## アイテムキャッチ（`frenchie-catch-game.tsx`）に新しいアイテムを追加・調整するとき

**必ず `docs/item-catch-new-item-checklist.md` を読んでから作業すること。** 追加手順・現在の目標値・
触ってはいけない数値がまとまっている早見表で、通常はこれだけで作業できる。判断がつかない場合だけ
`docs/minigame-time-balance.md` の「【2026-09-02 最新確定2】おかえりの1個あたり秒数を+1秒→+2秒に
変更」節と、その下にある「【2026-09-02 最新確定】時間増加系の出現カットオフ導入・新アイテム追加時は
まずここを見る」節を読む。それより下にある「【2026-09-02 再確定】現実的キャッチ率モデルへの切り替え」節、
およびさらにその下にある全ての節は更新前の内容なので参照しない。

要点（詳細・計算式・検証手順は `docs/item-catch-new-item-checklist.md` および上記docsを参照）:

- プレイ時間・スコアの検算は `node scripts/simulate-item-catch.mjs 1000 avoid` で行う
  （2026-09-03〜、ユーザー指定で試行回数は毎回1000でよい）。第4〜6引数（デフォルト: 時間増加系7種+おかえり
  80% / 通常時85% / 密集時50%）が現在の公式キャッチ率モデル。「画面に出た物は100%取れる」という
  前提には戻さないこと。**プレイ時間を目標値に合わせる調整作業は2026-09-03〜不要**（500秒超えが
  0%のままかだけ確認する）。
- 出現重みは、同ランク内で「そのランクの予算 ÷ 在籍数」の値をそのまま使う（2026-09-03〜、ユーザー指定。
  10の倍数への丸めは行わない）。
- 時間増加系7種＋おかえりは、プレイヤーの図鑑育成度（R以上アイテムのスキルLv平均）から決まる
  「カットオフ秒数」（Lv1相当=60秒〜Lv5相当=140秒、線形）を超えると出現しなくなる。これにより
  ラウンドが際限なく伸びることはなくなったため、**旧目標だった「Lv5で500秒超え10%等の裾の確率」は
  撤廃済み**（現在の基準値はdocsの検証結果表を参照）。
- 新アイテムを追加すると図鑑プールが増えて既存アイテムの取得確率が薄まるため、時間増加系7種の
  出現重みで相殺する必要がある。手順は docs 側の「新しいキャラ・アイテムを追加するときの手順」節の通り
  （カットオフ秒数自体はプール総数に依存しないので、この節の数値は新アイテム追加時に変更不要）。
- 数値を変えたら `frenchie-catch-game.tsx` の `LV` / `ITEM_SPAWN_WEIGHTS` と、
  `src/lib/games/item-catch-skills.ts` のルールブック説明文の**両方**を直す。
- 新アイテムは `src/lib/collection/items.ts`（図鑑本体）への追加を忘れないこと（ここに無いとミニゲームの
  `itemPool` に一切登場しない静かなバグになる）。
- 数値を確定させたら、`docs/minigame-time-balance.md` の基準値・検証結果も一緒に更新すること。
