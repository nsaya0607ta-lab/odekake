# アイテムキャッチ 新アイテム追加チェックリスト

新しいアイテムをアイテムキャッチ（`frenchie-catch-game.tsx`）に追加・調整するときに、まずこのファイルだけ見れば
作業できるようにした早見表。背景・計算式・過去の変更履歴は `docs/minigame-time-balance.md` にあるが、
毎回全部読む必要はない（このファイルの手順通りに直して検算すれば十分）。

## 現在のプレイ時間の目標値（2026-09-03〜、参考値。厳密に合わせる必要はない）

**2026-09-03、ユーザー指定でこの節は「参考値」に降格した。** 新アイテム追加のたびにこの数値へ
厳密に合わせる調整作業は不要。ランク予算・個々のアイテムの重みは
「同ランク内で均等に重みを割り振る計算方法」（下記）通りに機械的に決めればよく、
シミュレーション結果が下表から大きくズレていても許容する。

`node scripts/simulate-item-catch.mjs 20000 avoid` の実測値（2026-09-03最新。以下の変更を
すべて反映した後の版：時間増加系プールからおかえりの個別チューニング値(300)・出現量アップ
制御系プールからブレブルの固定値(20)を廃止しランク比率制に統合／プール外だった通常アイテム
61種を4つ目の「通常アイテム系プール」に統合／`dogWeight`の算出式を所持数近似から実重み合計
ベースに変更／得点倍率系プールのLR枠に新アイテム「ピンクオモ」を追加し漆黒のアーと均等割り。
未充填ランク分はdogへ上乗せしてプール予算を減らさない。N=90基準）。

**注意：ランク比率のみに統合した直後（時間増加系プール予算600のまま）はLv5平均プレイ時間が
181秒→95秒まで落ち込んだ。** おかえり(旧重み300)とブレブル(旧重み20)はいずれもプレイ時間の
体感バランスを保つために個別チューニングされていた値だったため、単純にランク比率へ揃えると
時間増加系の出現ペースが大きく下がってしまう。目標値を維持するため、時間増加系プールの予算を
600→2120（約3.5倍）に引き上げて再チューニングした（出現量アップ制御系プールはブレブル分の
再配分だけで従来予算900のまま維持できている）。その後`dogWeight`の算出式変更でLv5平均が
175秒→179秒とわずかに上振れしたが許容範囲内（詳細は`docs/minigame-time-balance.md`の
「最新確定6」節参照）。

| Lv | 秒数平均 | 秒数中央値 | スコア平均 | 500秒超え |
|---|---|---|---|---|
| 1 | 約75秒 | 約72秒 | 約24,000 | 0% |
| 2 | 約103秒 | 約102秒 | 約42,000 | 0% |
| 3 | 約126秒 | 約126秒 | 約63,000 | 0% |
| 4 | 約151秒 | 約154秒 | 約95,000 | 0% |
| 5 | 約179秒 | 約184秒 | 約144,000 | 0% |

**2026-09-03〜、上記の数値からのズレは気にしなくてよい**（ユーザー指定）。ただし500秒超えは
「時間増加系の出現カットオフ」（後述）が効いている限り基本的に0%のまま動かないはずなので、
0%以外の値が出たらカットオフ側の実装ミスを疑うこと（これは引き続き確認する）。

## 出現重みプール（追加時にどこを触るか迷ったらここを見る）

`frenchie-catch-game.tsx` の `ITEM_SPAWN_WEIGHTS` はフラットな1回抽選（全アイテムを1つの乱数で選ぶ）だが、
運用上は4つの「プール」に分けて考える（時間増加系・得点倍率系・出現量アップ制御系・通常アイテム系）。
**いずれもレアリティランクごとに固定比率（または固定予算）を割り当てる「ランク比率制」**
（2026-09-03〜、ユーザー指定。通常アイテム系のみN〜MRの7ランク、他3プールはR〜MRの6ランク）：

**R:30% / SR:18% / SSR:16% / UR:14% / LR:12% / MR:10%**（合計100%。通常アイテム系プールはこの
比率ではなく、現在の在籍数×100を各ランクの固定予算として直接管理する）

各プールの「予算」（時間増加系2120 / 得点倍率系400 / 出現量アップ・制御系900 / 通常アイテム系6100）に、
この比率を掛けた値がそのランクの予算。**2026-09-03、ユーザー指定で「固定重み」を全廃し、
どのアイテムも例外なく「そのランクの予算 ÷ そのランクに属するアイテムの人数」を丸めずそのまま
`ITEM_SPAWN_WEIGHTS`に書く1ルールに統一した**（個別チューニング値・10の倍数への丸めのどちらも
行わない）。ランク予算自体が割り切れない場合は、コード上に`予算 / 人数`の式をそのまま書いて計算させる
（例：`56 / 3`）。

**該当アイテムが無いランクの予算は、プールの合計から減らさず「普通のフレブル」(dog)の出現重みに
上乗せして消化する**（`TIME_BONUS_UNFILLED_RANK_DOG_WEIGHT` / `SCORE_MULT_UNFILLED_RANK_DOG_WEIGHT` /
`SPAWN_DYNAMICS_UNFILLED_RANK_DOG_WEIGHT`、`frenchie-catch-game.tsx`の`dogWeight`計算に加算。
`simulate-item-catch.mjs`側も同名定数で手動同期）。新しくそのランクにアイテムを追加したら、
対応する定数からそのランクの予算分を差し引き、`ITEM_SPAWN_WEIGHTS`にその予算（同ランクに複数いる
場合は均等割り、丸めなし）で追加する。

**注意：dogへの上乗せは「そのプールの体感的な取得ペースを維持する」ものではない。** dogは他の
全アイテムと同じ1回のフラット抽選に加わるため、dogWeight自体が大きくなるほど時間増加系アイテムを
含む全アイテムの相対選択確率がわずかに下がる（希釈される）。プレイ時間・スコアへの実際の影響は
`node scripts/simulate-item-catch.mjs`で必ず検算すること（ただし目標値への一致自体は不要、後述）。

プールの合計重み・未充填ランク分の内訳は`node scripts/simulate-item-catch.mjs`実行時に毎回
「プール重み予算: ...」として表示されるので、追加前後で比較すること。なお時間増加系プールの表示は
`TIME_BONUS_IDS`（おかえりを含まない7種のSet）ベースのため、実際にはおかえりの重みぶん少なく
表示される（実害はない表示上の仕様、詳細は下記）。

### 時間増加系7種＋おかえりプール（予算2120、在籍分の合計1187.2・残り932.8はdogへ）

| ランク | 比率 | 予算 | 現在の在籍アイテム | 1体あたり |
|---|---|---|---|---|
| R | 30% | 636 | あひるのぬいぐるみ / にんじんトイ / 肉球メロンパン | 636÷3=212 |
| SR | 18% | 381.6 | なし | ―（未充填→dog） |
| SSR | 16% | 339.2 | なし | ―（未充填→dog） |
| UR | 14% | 296.8 | アンボール / おもじぃ / 小豆(あずき) | 296.8÷3≒98.93 |
| LR | 12% | 254.4 | 夏のフレブル / おかえり | 254.4÷2=127.2 |
| MR | 10% | 212 | なし | ―（未充填→dog） |

定義：`TIME_BONUS_ITEM_IDS`（`frenchie-catch-game.tsx`）/ `TIME_BONUS_IDS`（`simulate-item-catch.mjs`）。
おかえり(`other_okaeri`)は`TIME_BONUS_ITEM_IDS`自体には含まれない（カットオフ対象への追加は
`TIME_BONUS_CUTOFF_ITEM_IDS`で別途行っている）が、`ITEM_SPAWN_WEIGHTS`の重みはこのプールのLR枠として
夏のフレブルと均等に割り振る。

### 得点倍率系8種＋ピンクオモ＋ナルシストアー＋マフィアープール（予算400、在籍分の合計280・残り120はdogへ）

| ランク | 比率 | 予算 | 現在の在籍アイテム | 1体あたり |
|---|---|---|---|---|
| R | 30% | 120 | なし | ―（未充填→dog） |
| SR | 18% | 72 | 大きな肉のおもちゃ / はるいろフラワーリース | 72÷2=36 |
| SSR | 16% | 64 | かむなよ / 二足アー | 64÷2=32 |
| UR | 14% | 56 | あずびー / きのこあずびー / こびー | 56÷3≒18.67 |
| LR | 12% | 48 | 漆黒のアー / ピンクオモ | 48÷2=24 |
| MR | 10% | 40 | ナルシストアー / マフィアー | 40÷2=20 |

定義：`SCORE_MULT_IDS`（`simulate-item-catch.mjs`のみ。得点倍率スキルを主効果とする8種＋ピンクオモ＋
ナルシストアー＋マフィアー）。宝箱・夏のフレブル・Xmas Partyは得点倍率効果も持つが、重みが別枠の
出現量アップ・制御系プールで管理されている「兼用アイテム」なので**このプールには含めない**
（`ITEM_SPAWN_WEIGHTS`直上のコメント参照）。ピンクオモ（画面ピンクフィルター＋アイテム引き寄せ）・
ナルシストアー（全アイテムのスキルがLv.MAXで発動）・マフィアー（フレブル数ボーナス倍率）は、
いずれも主効果が「得点倍率」そのものではないが、重み管理上の扱いとしてこのプールに含める
（ユーザー指定の運用上の例外）。

### 出現量アップ・出現制御系＋Listen to the a-プール（予算900、在籍分の合計774・残り126はdogへ）

| ランク | 比率 | 予算 | 現在の在籍アイテム | 1体あたり |
|---|---|---|---|---|
| R | 30% | 270 | ストレッチ棒 | 270÷1=270 |
| SR | 18% | 162 | 宝箱おやつパズル | 162÷1=162 |
| SSR | 16% | 144 | 虹色わんこボール / ぽんでおも / ぽんでアー / じゃれアー | 144÷4=36 |
| UR | 14% | 126 | なし | ―（未充填→dog） |
| LR | 12% | 108 | ラグビーアー / Listen to the a- | 108÷2=54 |
| MR | 10% | 90 | ブレブル / Xmas Party | 90÷2=45 |

定義：`SPAWN_DYNAMICS_ITEM_IDS`（`frenchie-catch-game.tsx`）/ `SPAWN_DYNAMICS_IDS`
（`simulate-item-catch.mjs`）。出現重みの計算式自体を書き換える系（UR確率アップ・レア限定・
出現量アップ等）。宝箱おやつパズル・ブレブル・Xmas Partyもかつては個別チューニング値だったが、
2026-09-03にユーザー指定で廃止し「ランク予算÷在籍数」のみに統一した。

### 通常アイテム系プール（予算6100、在籍61種すべて等分100ずつ）

2026-09-03、ユーザー指定で新設。特殊効果を持たない「通常アイテム」（得点のみ・演出のみ含む）を
`DEFAULT_ITEM_SPAWN_WEIGHT`任せの管理外状態から、他の3プールと同じ「レアリティ別ランク比率制」の
枠に入れた。新設時点ではいずれのランクも予算÷在籍数がちょうど100になるため、既存の重みからの
変更はなく、プレイ時間・スコアへの影響もゼロ。

| ランク | 予算 | 在籍数 | 1体あたり |
|---|---|---|---|
| N | 2400 | 24種 | 100 |
| R | 700 | 7種 | 100 |
| SR | 900 | 9種 | 100 |
| SSR | 1200 | 12種 | 100 |
| UR | 700 | 7種 | 100 |
| LR | 200 | 2種 | 100 |
| MR | 0 | 0種 | ―（未在籍。初めて追加するときに新規予算を設定しプール総予算6100に加算する） |

定義：`NORMAL_ITEM_IDS`（`frenchie-catch-game.tsx`）/ `NORMAL_ITEM_IDS`（`simulate-item-catch.mjs`、
手動同期）。**このプールに何種追加しても、プール総予算6100自体は変わらない**ため、他の3プールや
「普通のフレブル」(dog)の相対的な出現確率が薄まることはない（Nランクへの大量追加で時間増加系の
出現率が下がる、といった希釈は起きない設計）。

**2026-09-03、単独チューニング枠だった3つ（Listen to the a-・ナルシストアー・マフィアー）を
すべて上記4プールのいずれかに移動し、さらに宝箱おやつパズル・ブレブル・Xmas Partyの個別チューニング
値も廃止したため、現時点でプールに属さない単独チューニング枠・個別チューニング値は一切存在しない**
（ユーザー指定。Listen to the a-→出現量アップ・出現制御系プール、ナルシストアー・マフィアー→
得点倍率系プール）。

新アイテムは、性質に応じて上記4プールのいずれかに必ず追加すること
（`DEFAULT_ITEM_SPAWN_WEIGHT`のまま放置しない）。

### 同ランク内で均等に重みを割り振る計算方法（在籍ランクに追加するとき必ずこの手順で計算する）

**2026-09-03、ユーザー指定で簡略化。** 個々のアイテムの重みは「そのランクの予算 ÷ 新しい在籍数
（既存＋追加分）」をそのまま使う。丸めは一切行わない（割り切れない場合はコード上に`予算 / 人数`の式を
そのまま書き、計算結果の小数もそのまま使う）。例外・個別チューニングは今後一切設けない。

1. `そのランクの予算 ÷ 新しい在籍数（既存＋追加分）` を計算する。
2. その計算式（`予算 / 人数`）をそのまま在籍ランクの全メンバー分`ITEM_SPAWN_WEIGHTS`に書き込む
   （新規・既存問わず全員書き直す。数値ではなく式のまま書くことで、後から読んだときに
   「予算÷人数」であることが一目で分かるようにする）。

**計算例（得点倍率系プールLRランク、予算48に1種追加して2種にする場合）**
```
interior_shikkoku_no_ar: 48 / 2,
other_pink_omo: 48 / 2,
```

（旧・10の倍数丸めルールの記録：以前は「基準値を10の倍数に四捨五入し、差分を10刻みで一部+10する」
方式だったが、2026-09-03にユーザー指定で廃止した。さらに同日中に、個別チューニング値や単独枠を
残すこと自体もユーザー指定で全廃し、「ランク予算÷在籍数」の1ルールに完全統一した。）

### 新アイテムをプールに追加する手順

1. **性質に応じてどのプールに属するか判断する。**
2. **在籍ランクに追加する場合**：上記「同ランク内で均等に重みを割り振る計算方法」の手順通りに
   そのランクの予算を新しい人数で割り直し、`ITEM_SPAWN_WEIGHTS`の同ランクメンバー全員の重みを
   `予算 / 人数`の式で更新する（ランク比率・プール予算自体は変えない）。
3. **未充填ランクに追加する場合**：対応する`XXX_UNFILLED_RANK_DOG_WEIGHT`定数からそのランクの
   予算分を差し引き、`ITEM_SPAWN_WEIGHTS`に新アイテムをその予算で追加する
   （`frenchie-catch-game.tsx`・`simulate-item-catch.mjs`の両方を同期。同ランクに複数追加する場合は
   同様に「同ランク内で均等に重みを割り振る計算方法」で按分する）。
4. `simulate-item-catch.mjs`側のプールSet（`NORMAL_ITEM_IDS`含む）にも同じIDを追加する。
5. `node scripts/simulate-item-catch.mjs 1000 avoid` を実行し（2026-09-03〜、ユーザー指定で
   試行回数は毎回1000でよい）、「プール重み予算」行の各プールの合計（2120/400/900/6100）が
   変わっていないか確認する。**プレイ時間・スコアを目標値表に厳密に合わせる作業は不要。**
   500秒超えが0%のままかだけ確認する（0%でなくなったらカットオフ実装ミスを疑う）。
6. 得点倍率系プールに追加する場合は、重みだけでなく`SCORE_MULT_DURATION_*`（発動秒数）・倍率値も
   既存のレアリティ別カーブに揃えること（プールの重み予算を守っても、倍率値や持続時間が既存と
   ズレると得点倍率の重複掛け算リスクが変わってしまう）。

**特殊効果を持たない「通常アイテム」は、上記の「通常アイテム系プール」に追加する
（`DEFAULT_ITEM_SPAWN_WEIGHT`のまま放置しない）。** 現時点でどのプールにも属さない単独チューニング枠・
個別チューニング値は存在しない（2026-09-03、ユーザー指定で全廃済み）。

## 追加手順（毎回これをやる）

1. **`src/lib/collection/items.ts`（図鑑本体）に追加する。** ここに無いとミニゲームの`itemPool`に
   一切登場しない静かなバグになる（`prizes.ts`や`item-catch-skills.ts`だけ追加して忘れるケースが過去に実例あり）。
2. **`src/lib/games/item-catch-skills.ts`にルールブック説明文を追加する。**
3. アイテムの性質によって、`frenchie-catch-game.tsx`側で以下を判断する：
   - **時間を直接増やすスキル**（「時間増加系」）にする場合 → `TIME_BONUS_ITEM_IDS`に追加し、
     `LV`テーブルにLv1〜5の秒数（単調増加）を追加。**さらに`TIME_BONUS_CUTOFF_ITEM_IDS`にも追加される
     ことを確認する**（`TIME_BONUS_ITEM_IDS`から自動的に含まれるので通常は何もしなくてよいが、
     `おかえり`のように別枠で追加している特殊ケースだけ手動追加が必要）
   - **出現量アップ系**にする場合 → `SPAWN_DYNAMICS_ITEM_IDS`に追加し、`LV.xxx_SPAWN`配列（Lv1〜5の倍率）を追加
   - **それ以外**（得点系・演出系など） → 上記どちらにも追加しない
4. `ITEM_SPAWN_WEIGHTS`に出現重みを設定する（未指定はデフォルト100）。**新アイテムが1つ増えると
   プール総数が増え、既存アイテム（時間増加系7種＋おかえりを含む）の相対確率が薄まる。**
   完全に相殺したい場合は、既存の時間増加系7種＋おかえりの重みを「プール総数の増加率」
   （`Total(N+1)/Total(N)`）だけ底上げする。目安として、N=89→90程度の増加なら1%前後の底上げで足りる。
5. 数値を変えたら、`LV` / `ITEM_SPAWN_WEIGHTS`（と出現量アップ系なら`LV.xxx_SPAWN`）を
   `frenchie-catch-game.tsx`側で直し、`item-catch-skills.ts`のルールブック説明文と数値を一致させる
   （片方だけ直すと表記と実装がズレる）。
6. `node scripts/simulate-item-catch.mjs 1000 avoid` を実行する（2026-09-03〜、ユーザー指定で
   試行回数は毎回1000でよい。プレイ時間の目標値への一致は不要、500秒超えが0%のままかだけ確認する）。
7. 実行結果を `docs/minigame-time-balance.md` にも追記する（次回の基準がズレないように）。

## 「時間増加系の出現カットオフ」を壊さないための注意

- プレイヤーの図鑑育成度（R以上アイテムのスキルLv平均）に応じて、プレイ時間が一定秒数
  （平均Lv1→60秒〜平均Lv5→140秒）を超えると時間増加系7種＋おかえりが出現しなくなる仕組みが入っている
  （`TIME_BONUS_CUTOFF_ITEM_IDS` / `timeBonusCutoffSecRef`、詳細は`docs/minigame-time-balance.md`の
  「時間増加系の出現カットオフ導入」節）。
- 新アイテムを時間増加系に追加するときは、この`TIME_BONUS_CUTOFF_ITEM_IDS`（`TIME_BONUS_ITEM_IDS`から
  自動的に含まれる）に確実に入るようにすること。ここに入れ忘れると、カットオフ後もそのアイテムだけ
  出現し続け、プレイ時間が際限なく伸びる不具合に逆戻りする。
- 宝箱(`toy_treasure_puzzle`)・？アイテムはこのカットオフの対象外（意図的な例外なので触らない）。

## 触ってはいけない・慎重に扱うもの

- `MAX_ROUND_SECONDS = 1800`（30分の強制終了）：保険なので削除しない。
- **（2026-09-03廃止）** ナルシストアー/マフィアーの出現重み20固定は、2026-09-03にユーザー指定で
  得点倍率系プールのMR枠（予算40÷在籍2種=20ずつ）に統合済み。結果として値自体は20のまま変わって
  いないが、今後は「単独の固定値」ではなく「得点倍率系プールMR枠の予算配分」として扱うこと
  （MR枠に他のアイテムを追加する場合はこの2つも含めて按分し直す）。
- **（2026-09-03廃止）** 宝箱(`toy_treasure_puzzle`)の重みの個別チューニングは、ユーザー指定で
  出現量アップ・制御系プールSR枠（予算162÷在籍1種=162）に統合済み。今後「触ってはいけない個別値」
  として扱わないこと。
- **（2026-09-03廃止）** おかえり(`other_okaeri`)の重み300・ブレブル(`other_burebur`)の重み20固定は
  ユーザー指定でランク比率制のみに統合済み。今後この2つを「触ってはいけない個別値」として扱わないこと
  （代わりに時間増加系プールの予算2120自体を、新アイテム追加時の相殺対象として扱う）。
- **（2026-09-03廃止）** Xmas Party・ラグビーアーの重みも同日、ユーザー指定で個別チューニングを廃止し
  それぞれ出現量アップ・制御系プールのMR枠・LR枠の「予算÷在籍数」に統合済み。
- 上記の通り、2026-09-03時点で「触ってはいけない個別重み」は0件になった。今後は例外を作らず、
  常に「そのランクの予算 ÷ 在籍数」の1ルールだけで計算すること。

## 迷ったら

`docs/minigame-time-balance.md`の一番上の節（「【2026-09-02 最新確定2】」「【2026-09-02 最新確定】」）に
計算式・検証結果・過去の失敗例が詳しく書いてある。このチェックリストだけで判断がつかない場合はそちらを参照する。
