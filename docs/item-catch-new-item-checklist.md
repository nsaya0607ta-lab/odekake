# アイテムキャッチ 新アイテム追加チェックリスト

新しいアイテムをアイテムキャッチ（`frenchie-catch-game.tsx`）に追加・調整するときに、まずこのファイルだけ見れば
作業できるようにした早見表。背景・計算式・過去の変更履歴は `docs/minigame-time-balance.md` にあるが、
毎回全部読む必要はない（このファイルの手順通りに直して検算すれば十分）。

## 現在のプレイ時間の目標値（この数値から大きくズレないようにする）

`node scripts/simulate-item-catch.mjs 20000 avoid` の実測値（2026-09-03、3プールとも
レアリティ別ランク予算制＋未充填ランク分をdogへ上乗せ・N=89基準）。

| Lv | 秒数平均 | 秒数中央値 | スコア平均 | 500秒超え |
|---|---|---|---|---|
| 1 | 約88秒 | 約86秒 | 約31,000 | 0% |
| 2 | 約116秒 | 約114秒 | 約50,000 | 0% |
| 3 | 約141秒 | 約141秒 | 約76,000 | 0% |
| 4 | 約169秒 | 約170秒 | 約115,000 | 0% |
| 5 | 約204秒 | 約206秒 | 約181,000 | 0.01% |

**±10〜15%程度のズレは許容範囲。** それ以上ズレたら下記手順で相殺すること。500秒超えは
「時間増加系の出現カットオフ」（後述）が効いている限り基本的に0%のまま動かないはずなので、
0%以外の値が出たらカットオフ側の実装ミスを疑うこと。

## 出現重みプール（追加時にどこを触るか迷ったらここを見る）

`frenchie-catch-game.tsx` の `ITEM_SPAWN_WEIGHTS` はフラットな1回抽選（全アイテムを1つの乱数で選ぶ）だが、
運用上は3つの「プール」に分けて考える。**3プールとも、レアリティランクごとに固定予算を割り当てる
「ランク予算制」**（2026-09-03〜、ユーザー指定）。ランク予算は必ず**10の倍数**にし、そのランクに
属するアイテムの人数で均等割りした「1体あたり」の重みも必ず10の倍数になるようにする
（1桁目が1や2などの半端な値にしない）。

該当アイテムがまだ存在しないランクの予算は、そのぶんを空費させず**「普通のフレブル」(dog)の出現重みに
上乗せして消化する**（`XXX_UNFILLED_RANK_DOG_WEIGHT`定数、`frenchie-catch-game.tsx`の`dogWeight`計算に
加算。`simulate-item-catch.mjs`側も同名定数で手動同期）。新しくそのランクのアイテムを追加したら、
対応する`UNFILLED_RANK_DOG_WEIGHT`定数からそのランクの予算を差し引き、`ITEM_SPAWN_WEIGHTS`に
その予算（同ランクに複数いる場合は均等割り、10の倍数を維持）で追加する。

プールの合計重み・未充填ランクの内訳は`node scripts/simulate-item-catch.mjs`実行時に毎回
「プール重み予算: ...」として表示されるので、追加前後で比較すること。

### 時間増加系7種プール（合計600）

| ランク | 予算 | 現在の在籍アイテム | 1体あたり |
|---|---|---|---|
| R | 270 | あひるのぬいぐるみ / にんじんトイ / 肉球メロンパン | 90 |
| SR | 10 | なし | ―（未充填→dog） |
| SSR | 10 | なし | ―（未充填→dog） |
| UR | 210 | アンボール / おもじぃ / 小豆(あずき) | 70 |
| LR | 90 | 夏のフレブル | 90 |
| MR | 10 | なし | ―（未充填→dog） |
| **合計** | **600** | | 未充填分30は`TIME_BONUS_UNFILLED_RANK_DOG_WEIGHT`でdogへ |

定義：`TIME_BONUS_ITEM_IDS`（`frenchie-catch-game.tsx`）/ `TIME_BONUS_IDS`（`simulate-item-catch.mjs`）。
おかえり(`other_okaeri`)は時間バランス調整用に重み300で個別チューニング済みのため、このプールには含めない。

### 得点倍率系8種プール（合計400）

| ランク | 予算 | 現在の在籍アイテム | 1体あたり |
|---|---|---|---|
| R | 100 | なし | ―（未充填→dog） |
| SR | 90 | 大きな肉のおもちゃ / はるいろフラワーリース | 45※ |
| SSR | 80 | かむなよ / 二足アー | 40 |
| UR | 60 | あずびー / きのこあずびー / こびー | 20 |
| LR | 40 | 漆黒のアー | 40 |
| MR | 30 | なし | ―（未充填→dog） |
| **合計** | **400** | | 未充填分130は`SCORE_MULT_UNFILLED_RANK_DOG_WEIGHT`でdogへ |

※SRのみ2026-09-03のランク予算制導入前に決めた値のため10の倍数になっていない（後から統一する場合は
SR予算90を2体で割り切れる値に変更するか3体構成にすること）。

定義：`SCORE_MULT_IDS`（`simulate-item-catch.mjs`のみ。得点倍率スキルを主効果とする8種）。
宝箱・夏のフレブル・Xmas Partyは得点倍率効果も持つが、重みが別枠でチューニング済みの「兼用アイテム」
なので**このプールには含めない**（`ITEM_SPAWN_WEIGHTS`直上のコメント参照）。

### 出現量アップ・出現制御系プール（合計900）

| ランク | 予算 | 現在の在籍アイテム | 1体あたり |
|---|---|---|---|
| R | 100 | ストレッチ棒 | 100 |
| SR | 150 | 宝箱おやつパズル | 150 |
| SSR | 400 | 虹色わんこボール / ぽんでおも / ぽんでアー / じゃれアー | 100 |
| UR | 30 | なし | ―（未充填→dog） |
| LR | 100 | ラグビーアー | 100 |
| MR | 120 | ブレブル(20・固定/触らない) / Xmas Party(100) | ―（ブレブルのみ固定） |
| **合計** | **900** | | 未充填分30は`SPAWN_DYNAMICS_UNFILLED_RANK_DOG_WEIGHT`でdogへ |

定義：`SPAWN_DYNAMICS_ITEM_IDS`（`frenchie-catch-game.tsx`）/ `SPAWN_DYNAMICS_IDS`
（`simulate-item-catch.mjs`）。出現重みの計算式自体を書き換える系（UR確率アップ・レア限定・
出現量アップ等）。**ブレブルの重み20は「触ってはいけない」対象**（`CLAUDE.md`参照）なので、
MRランク予算からブレブル分20を引いた残りをXmas Partyに割り当てる。

### 新アイテムをプールに追加する手順

1. **性質に応じてどのプールに属するか判断する。**
2. **在籍ランクに追加する場合**：そのランクの予算を新しい人数で割り直し、`ITEM_SPAWN_WEIGHTS`の
   同ランクメンバー全員の重みを更新する（10の倍数を維持。ランク予算自体は変えない）。
3. **未充填ランクに追加する場合**：対応する`UNFILLED_RANK_DOG_WEIGHT`定数からそのランクの予算を
   差し引き、`ITEM_SPAWN_WEIGHTS`に新アイテムをそのランクの予算で追加する
   （`frenchie-catch-game.tsx`・`simulate-item-catch.mjs`の両方を同期）。
4. `simulate-item-catch.mjs`側のプールSet（該当する場合）にも同じIDを追加する。
5. `node scripts/simulate-item-catch.mjs 8000 avoid` を実行し、「プール重み予算」行の
   各プールの合計（600/400/900）が変わっていないか確認する。
6. 得点倍率系プールに追加する場合は、重みだけでなく`SCORE_MULT_DURATION_*`（発動秒数）・倍率値も
   既存のレアリティ別カーブに揃えること（プールの重み予算を守っても、倍率値や持続時間が既存と
   ズレると得点倍率の重複掛け算リスクが変わってしまう）。

**プールに属さないアイテム**（得点系単発・演出系など）は`DEFAULT_ITEM_SPAWN_WEIGHT`（100）のままでよく、
プールの合計重みには影響しない。

## 追加手順（毎回これをやる）

1. **`src/lib/collection/items.ts`（図鑑本体）に追加する。** ここに無いとミニゲームの`itemPool`に
   一切登場しない静かなバグになる（`prizes.ts`や`item-catch-skills.ts`だけ追加して忘れるケースが過去に実例あり）。
2. **`src/lib/games/item-catch-skills.ts`にルールブック説明文を追加する。**
3. アイテムの性質によって、`frenchie-catch-game.tsx`側で以下を判断する：
   - **時間を直接増やすスキル**（「時間増加系」）にする場合 → `TIME_BONUS_ITEM_IDS`に追加し、
     `LV`テーブルにLv1〜5の秒数（単調増加）を追加。**さらに`TIME_BONUS_CUTOFF_ITEM_IDS`にも追加される
     ことを確認する**（`TIME_BONUS_ITEM_IDS`から自動的に含まれるので通常は何もしなくてよいが、
     `おかえり`のように別枠で追加している特殊ケースだけ手動追加が必要）
   - **出現量アップ系**にする場合 → `SPAWN_DYNAMICS_ITEM_IDS`に追加し、`LV.xxx_SPAWN`配列（Lv1〜5の倍率）を追加
   - **それ以外**（得点系・演出系など） → 上記どちらにも追加しない
4. `ITEM_SPAWN_WEIGHTS`に出現重みを設定する（未指定はデフォルト100）。**新アイテムが1つ増えると
   プール総数が増え、既存アイテム（時間増加系7種＋おかえりを含む）の相対確率が薄まる。**
   完全に相殺したい場合は、既存の時間増加系7種＋おかえりの重みを「プール総数の増加率」
   （`Total(N+1)/Total(N)`）だけ底上げする。目安として、N=89→90程度の増加なら1%前後の底上げで足りる。
5. 数値を変えたら、`LV` / `ITEM_SPAWN_WEIGHTS`（と出現量アップ系なら`LV.xxx_SPAWN`）を
   `frenchie-catch-game.tsx`側で直し、`item-catch-skills.ts`のルールブック説明文と数値を一致させる
   （片方だけ直すと表記と実装がズレる）。
6. `node scripts/simulate-item-catch.mjs 8000 avoid` を実行し、上記の目標値表と見比べる：
   - 全レベル（Lv1〜5すべて。Lv5だけでなく中間レベルもズレることがある）で秒数が単調増加しているか
   - 500秒超えが0%のままか（0%でなくなったらカットオフ実装を疑う）
   - 目標値から大きくズレていたら、手順4の重み調整（時間増加系7種＋おかえりの重み）で微調整する
7. 検算した数値を `docs/minigame-time-balance.md` にも追記する（次回の基準がズレないように）。

## 「時間増加系の出現カットオフ」を壊さないための注意

- プレイヤーの図鑑育成度（R以上アイテムのスキルLv平均）に応じて、プレイ時間が一定秒数
  （平均Lv1→60秒〜平均Lv5→140秒）を超えると時間増加系7種＋おかえりが出現しなくなる仕組みが入っている
  （`TIME_BONUS_CUTOFF_ITEM_IDS` / `timeBonusCutoffSecRef`、詳細は`docs/minigame-time-balance.md`の
  「時間増加系の出現カットオフ導入」節）。
- 新アイテムを時間増加系に追加するときは、この`TIME_BONUS_CUTOFF_ITEM_IDS`（`TIME_BONUS_ITEM_IDS`から
  自動的に含まれる）に確実に入るようにすること。ここに入れ忘れると、カットオフ後もそのアイテムだけ
  出現し続け、プレイ時間が際限なく伸びる不具合に逆戻りする。
- 宝箱(`toy_treasure_puzzle`)・？アイテムはこのカットオフの対象外（意図的な例外なので触らない）。

## 触ってはいけない・慎重に扱うもの

- `MAX_ROUND_SECONDS = 1800`（30分の強制終了）：保険なので削除しない。
- MR3種（ブレブル/ナルシストアー/マフィアー）の出現重み20：得点倍率スタックの暴走対策で意図的に低くしてある。
  新アイテム追加でここを一律の値に巻き込まないこと。
- おかえり(`other_okaeri`)・宝箱(`toy_treasure_puzzle`)の重み：時間バランス調整用に個別チューニング済み
  （レアリティ相応の値ではない）。新アイテム追加の相殺以外の理由で変更しない。

## 迷ったら

`docs/minigame-time-balance.md`の一番上の節（「【2026-09-02 最新確定2】」「【2026-09-02 最新確定】」）に
計算式・検証結果・過去の失敗例が詳しく書いてある。このチェックリストだけで判断がつかない場合はそちらを参照する。
